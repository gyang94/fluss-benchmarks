# Fluss connection configuration
# Update these values to match your Fluss cluster

fluss.bootstrap.servers=<YOUR_BOOTSTRAP_SERVERS>

# Optional: SASL authentication
# fluss.security.protocol=SASL
# fluss.security.sasl.mechanism=PLAIN
# fluss.security.sasl.username=<YOUR_USERNAME>
# fluss.security.sasl.password=<YOUR_PASSWORD>

# Optional: bucket count (default: 30)
# fluss.bucket.num=30
